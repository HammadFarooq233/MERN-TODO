pipeline {
    agent any

    stages {
        stage ("Build Backend") {
            steps {
                sh "ls"
            }
        }

        stage ("Build Frontend") {
            steps {
                sh "ls"
            }
        }
    }
}