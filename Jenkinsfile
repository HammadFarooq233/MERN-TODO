pipeline {
    agent any

    stages {
        stage ("Build Backend") {
            steps {
                sh "ls"
                cd "todo-backend"
            }
        }

        stage ("Build Frontend") {
            steps {
                sh "ls"
            }
        }
    }
}